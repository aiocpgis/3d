function enhanceTransitionCards(){document.querySelectorAll('.effect-card').forEach(card=>{const stage=card.querySelector('.demo-stage');if(!stage||stage.dataset.transitionReady)return;stage.dataset.transitionReady='true';const animate=document.createElement('button');animate.type='button';animate.className='transition-animate-btn';animate.textContent='Animate';animate.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();card.classList.remove('transition-animated');void card.offsetWidth;card.classList.add('transition-animated');setTimeout(()=>card.classList.remove('transition-animated'),950)});stage.appendChild(animate);const dot=document.createElement('button');dot.type='button';dot.className='transition-copy-dot';dot.setAttribute('aria-label','Copy prompt');dot.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();card.querySelector('.copy-prompt-btn')?.click();dot.textContent='✓';setTimeout(()=>dot.textContent='',900)});stage.appendChild(dot)})}
function enhanceThemeNav(){const nav=document.querySelector('.nav-links');if(!nav||nav.dataset.transitionNav)return;nav.dataset.transitionNav='true';const skill=document.createElement('a');skill.href='studio.html';skill.textContent='Skill';const likes=document.createElement('a');likes.href='favorites.html';likes.textContent='1.1k';nav.append(skill,likes)}
function bootTransitionsTheme(){enhanceThemeNav();enhanceTransitionCards();setTimeout(enhanceTransitionCards,200);setTimeout(enhanceTransitionCards,700)}
const transitionObserver=new MutationObserver(()=>enhanceTransitionCards());
if(document.body)transitionObserver.observe(document.body,{childList:true,subtree:true});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bootTransitionsTheme);else bootTransitionsTheme();